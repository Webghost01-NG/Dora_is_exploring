PORT=3002
DB_HOST=localhost
DB_PORT=5432
DB_NAME=explorer
DB_USER=postgres
DB_PASSWORD=postgres

# Plain HTTPS RPC is fine here - no subscriptions needed, just getCode() calls.
# Get a free one from Alchemy/Infura/QuickNode, or use a public one for testing:
RPC_HTTP_URL=https://ethereum-sepolia-rpc.publicnode.com
