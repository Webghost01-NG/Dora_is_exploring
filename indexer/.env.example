# RPC endpoint - use a WebSocket URL (wss://) not https, since we need block subscriptions
RPC_WS_URL=wss://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Postgres connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=explorer
DB_USER=postgres
DB_PASSWORD=postgres
